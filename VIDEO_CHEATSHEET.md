# 🎬 ШПАРГАЛКА: Обработка видео

## ⚡ БЫСТРЫЕ КОМАНДЫ

### Одно видео:
```powershell
python detect_violations.py --source "video.mp4" --no-display
```

### Папка с видео:
```powershell
python detect_violations.py --batch "videos_to_process" --no-display
```

### Веб-камера:
```powershell
python detect_violations.py --source 0
```

### Без сохранения полного видео:
```powershell
python detect_violations.py --source "video.mp4" --no-save --no-display
```

---

## 📂 Куда класть видео

```
videos_to_process/
├── lesson1.mp4
├── lesson2.mp4
└── exam.mp4
```

## 📂 Где результаты

```
output/
├── processed_videos/  ← Полные обработанные видео
├── videos/            ← Фрагменты нарушений
├── faces/             ← Фото лиц
└── report_*.json      ← Отчёты
```

---

## 🎯 Параметры

| Параметр | Описание |
|----------|----------|
| `--source PATH` | Путь к видео или 0 (камера) |
| `--batch PATH` | Папка с видео |
| `--model PATH` | Путь к модели |
| `--no-display` | Без окна (быстрее) |
| `--no-save` | Не сохранять полное видео |

---

## ⚡ Производительность

**На GPU (RTX 5070):**
- Видео 10 мин → 1-2 мин обработки

**На CPU:**
- Видео 10 мин → 10-20 мин обработки

---

## 📊 Что в отчёте

```json
{
  "id": 1,
  "type": "food_and_water",
  "duration_seconds": 27,
  "person_name": "Иван Иванов",
  "confidence": 0.89,
  "video_path": "output/videos/...",
  "face_path": "output/faces/..."
}
```

---

## 🚀 НАЧАТЬ СЕЙЧАС:

```powershell
# 1. Положите видео
copy "video.mp4" "videos_to_process\"

# 2. Запустите
python detect_violations.py --batch "videos_to_process" --no-display

# 3. Смотрите результаты
explorer output
```

**Готово!** 🎉
