# 🚀 ИНСТРУКЦИЯ ПО ЗАПУСКУ ОБУЧЕНИЯ

## 📋 Пошаговое руководство

### Шаг 1: Проверка системы (ОБЯЗАТЕЛЬНО!)

Перед обучением запустите проверку:

```powershell
python check_system.py
```

**Что проверяется:**
- ✅ Доступность GPU (CUDA)
- ✅ Наличие датасета и изображений
- ✅ Установка всех необходимых пакетов
- ✅ Правильность конфигурации

**Ожидаемый результат:**
```
🎉 ВСЁ ГОТОВО К ОБУЧЕНИЮ!

🚀 Запустите обучение командой:
   python train_model.py

⚡ С вашей GPU обучение займёт ~30-45 минут (100 эпох)
```

---

### Шаг 2: Запуск обучения

После успешной проверки запустите обучение:

```powershell
python train_model.py
```

**Что произойдёт:**

1. **Загрузка модели** (5-10 сек)
   ```
   📥 Загрузка модели: yolo11n.pt
   ```

2. **Проверка GPU** (1 сек)
   ```
   🎮 GPU обнаружена: NVIDIA GeForce RTX 5070
   💾 Память GPU: 12.00 GB
   ✅ Обучение будет на GPU (быстро!)
   ```

3. **Настройка параметров** (1 сек)
   ```
   🎯 Параметры для GPU: эпохи=100, batch=32, размер=640
   ```

4. **Обучение** (30-45 минут на GPU, несколько часов на CPU)
   ```
   Epoch 1/100: 100%|████████| loss: 0.89
   Epoch 2/100: 100%|████████| loss: 0.72
   ...
   Epoch 100/100: 100%|████████| loss: 0.15
   ```

5. **Валидация** (1-2 минуты)
   ```
   📊 Валидация модели...
   📈 Метрики модели:
     mAP50: 0.9234
     mAP50-95: 0.6789
     Precision: 0.8912
     Recall: 0.8456
   ```

6. **Экспорт модели** (5 сек)
   ```
   📦 Экспорт модели...
   ✅ Модель скопирована в: models/best.pt
   ```

---

### Шаг 3: Результаты обучения

После завершения вы получите:

```
models/
└── discipline_monitor/
    ├── weights/
    │   ├── best.pt         ← 🎯 Лучшая модель (используйте её!)
    │   └── last.pt         ← Последняя эпоха
    ├── results.png         ← 📊 Графики метрик
    ├── confusion_matrix.png ← Матрица ошибок
    ├── BoxPR_curve.png     ← Precision-Recall кривая
    └── train_batch*.jpg    ← Примеры обучающих данных
```

**Самый важный файл:** `models/discipline_monitor/weights/best.pt`

Этот файл автоматически копируется в `models/best.pt` и будет использоваться для детекции.

---

## 📊 Понимание метрик

### mAP50 (mean Average Precision @ IoU=0.50)
- **Что это:** Средняя точность детекции при пороге IoU 50%
- **Хорошо:** > 0.80
- **Отлично:** > 0.90
- **Что значит:** Насколько точно модель находит объекты

### mAP50-95
- **Что это:** Средняя точность при порогах IoU от 50% до 95%
- **Хорошо:** > 0.60
- **Отлично:** > 0.75
- **Что значит:** Насколько точно модель определяет границы объектов

### Precision (Точность)
- **Что это:** Доля правильных детекций среди всех детекций
- **Хорошо:** > 0.75
- **Отлично:** > 0.85
- **Что значит:** Мало ложных срабатываний

### Recall (Полнота)
- **Что это:** Доля найденных объектов среди всех реальных
- **Хорошо:** > 0.70
- **Отлично:** > 0.80
- **Что значит:** Модель находит большинство нарушений

---

## ⚙️ Параметры обучения

### Автоматически выбираемые параметры:

**Для GPU (RTX 5070, 12GB):**
- Эпохи: 100
- Batch size: 32
- Размер изображения: 640x640
- Время: ~30-45 минут

**Для CPU:**
- Эпохи: 20
- Batch size: 8
- Размер изображения: 320x320
- Время: несколько часов

### Если хотите изменить:

Откройте `config.py` и измените:

```python
# Для более быстрого обучения (меньше качество):
TRAIN_EPOCHS = 50
TRAIN_BATCH = 16
TRAIN_IMG_SIZE = 512

# Для максимального качества (дольше):
TRAIN_EPOCHS = 200
TRAIN_BATCH = 16
TRAIN_IMG_SIZE = 1024

# Для баланса (рекомендуется):
TRAIN_EPOCHS = 100
TRAIN_BATCH = 32
TRAIN_IMG_SIZE = 640
```

---

## 🎯 После обучения: Тестирование модели

### 1. Быстрый тест на одном изображении

```powershell
python -c "from ultralytics import YOLO; model = YOLO('models/best.pt'); model.predict('system_monitoring2-1/test/images/какое-то-изображение.jpg', save=True, conf=0.5)"
```

Результат сохранится в `runs/detect/predict/`

### 2. Тест на видео

```powershell
python detect_violations.py --source "путь/к/видео.mp4" --model models/best.pt
```

### 3. Тест на веб-камере

```powershell
python detect_violations.py --source 0 --model models/best.pt
```

---

## 🔍 Мониторинг во время обучения

### Отслеживание использования GPU

Откройте **второй терминал** и запустите:

```powershell
nvidia-smi -l 1
```

**Что вы увидите:**
```
+-----------------------------------------------------------------------------+
| NVIDIA-SMI 566.03       Driver Version: 566.03       CUDA Version: 12.7     |
|-------------------------------+----------------------+----------------------+
| GPU  Name            TCC/WDDM | Bus-Id        Disp.A | Volatile Uncorr. ECC |
| Fan  Temp  Perf  Pwr:Usage/Cap|         Memory-Usage | GPU-Util  Compute M. |
|===============================+======================+======================|
|   0  NVIDIA GeForce ... WDDM  | 00000000:01:00.0  On |                  N/A |
| 30%   65C    P2   180W / 250W |   9856MiB / 12288MiB |     98%      Default |
+-------------------------------+----------------------+----------------------+
```

**Важные показатели:**
- **GPU-Util:** Должен быть ~95-100% (GPU работает на полную)
- **Memory-Usage:** ~8-10 GB из 12 GB (нормально)
- **Temp:** 60-75°C (нормально, <85°C безопасно)
- **Power:** ~150-200W (нормально)

---

## ❗ Troubleshooting (Решение проблем)

### Проблема: "CUDA out of memory"

**Решение 1:** Уменьшите batch size в `train_model.py`:
```python
batch = 16  # вместо 32
```

**Решение 2:** Уменьшите размер изображения:
```python
imgsz = 512  # вместо 640
```

### Проблема: "GPU не используется"

**Проверка:**
```powershell
python check_gpu.py
```

**Если CUDA недоступна:**
1. Проверьте драйверы NVIDIA
2. Переустановите PyTorch:
   ```powershell
   pip uninstall torch torchvision -y
   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
   ```

### Проблема: "No module named 'ultralytics'"

**Решение:**
```powershell
pip install ultralytics
```

### Проблема: Низкие метрики после обучения

**Возможные причины:**
1. **Мало данных:** Добавьте больше изображений (минимум 100+ на класс)
2. **Мало эпох:** Увеличьте количество эпох до 150-200
3. **Неправильная разметка:** Проверьте качество аннотаций

**Решение:**
- Увеличьте `TRAIN_EPOCHS = 150`
- Используйте аугментацию (уже включена автоматически)
- Проверьте датасет на ошибки

### Проблема: Обучение слишком медленное

**На GPU должно быть быстро!** Если медленно:
1. Проверьте, что используется GPU: `python check_gpu.py`
2. Закройте другие приложения, использующие GPU
3. Уменьшите размер изображений до 512

**На CPU будет медленно** - это нормально. Ожидайте несколько часов.

---

## 📞 Дополнительная помощь

### Логи обучения

Все логи сохраняются в:
```
models/discipline_monitor/
```

### Документация

- `START_HERE.md` - Общая информация
- `SETUP_COMPLETE.md` - Настройка системы
- `README_YOLO.md` - Полная документация
- `TODO.md` - Чеклист задач

### Полезные команды

```powershell
# Проверка системы
python check_system.py

# Проверка GPU
python check_gpu.py

# Обучение
python train_model.py

# Тест детекции
python detect_violations.py --source 0
```

---

## 🎉 Готово!

Теперь вы готовы к обучению модели!

**Следующие шаги:**
1. ✅ `python check_system.py` - проверка
2. ✅ `python train_model.py` - обучение (30-45 мин на GPU)
3. ✅ `python detect_violations.py --source 0` - тестирование

**Удачи! 🚀**
