# 🎯 Система мониторинга дисциплины на учебных занятиях

Автоматическое обнаружение нарушений дисциплины с использованием YOLO11 и распознавания лиц.

## 📋 Описание

Система детектирует следующие нарушения:
- **Сон на занятии** - закрытые глаза более N секунд
- **Использование телефона** - обнаружение телефона в руках
- **Запрещённые предметы** - еда, напитки и т.д.

## 🚀 Возможности

✅ Детекция нарушений в реальном времени  
✅ Запись видеофрагментов при обнаружении  
✅ Распознавание лиц нарушителей  
✅ Автоматическое формирование отчетов  
✅ Работа с веб-камерой, видеофайлами и IP-камерами  

## 📦 Установка

### 1. Создание виртуального окружения

```powershell
python -m venv .venv
.venv\Scripts\activate
```

### 2. Установка зависимостей

```powershell
pip install -r requirements_yolo.txt
```

### 3. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```powershell
copy .env.example .env
```

Отредактируйте `.env` и добавьте свой API ключ Roboflow:

```
ROBOFLOW_API_KEY=ваш_ключ_здесь
ROBOFLOW_WORKSPACE=ваш_workspace
ROBOFLOW_PROJECT=название_проекта
ROBOFLOW_VERSION=1
```

## 🎓 Обучение модели

### 1. Загрузка датасета и обучение

```powershell
python train_model.py
```

Скрипт выполнит:
- Загрузку датасета с Roboflow
- Обучение модели YOLO11
- Валидацию модели
- Экспорт обученной модели

## 👤 Управление базой данных лиц

### Добавление студентов в базу данных

```powershell
python face_database.py
```

Команды:
- `add` - добавить одно фото
- `add_folder` - добавить папку с фото одного человека
- `list` - показать всех в базе
- `remove` - удалить человека
- `exit` - выход

### Пример добавления студента:

```
Введите команду: add_folder
Имя студента: Иванов Иван
Путь к папке с фото: D:\photos\ivanov\
```

## 🎬 Запуск детекции

### Детекция с веб-камеры

```powershell
python detect_violations.py --source 0
```

### Детекция из видеофайла

```powershell
python detect_violations.py --source path/to/video.mp4
```

### Детекция с IP-камеры

```powershell
python detect_violations.py --source rtsp://username:password@ip:port/stream
```

### С указанием модели

```powershell
python detect_violations.py --model models/best.pt --source 0
```

## 📊 Результаты

После завершения работы система генерирует:

### 📁 Структура выходных данных:

```
output/
├── videos/              # Видеозаписи нарушений
│   ├── food_and_water_1_20250101_120000.mp4
│   └── sleep_and_phone_2_20250101_120530.mp4
├── faces/               # Изображения лиц нарушителей
│   ├── 1_20250101_120000_face.jpg
│   └── 2_20250101_120530_face.jpg
└── report_20250101_123000.json  # Отчет в JSON
```

### 📄 Формат отчета:

```json
[
  {
    "id": 1,
    "type": "sleep_and_phone",
    "start_time": "2025-01-01T12:00:00",
    "end_time": "2025-01-01T12:00:45",
    "duration_seconds": 45.0,
    "person_name": "Иванов Иван",
    "confidence": 0.89,
    "video_path": "output/videos/sleep_and_phone_1.mp4",
    "face_path": "output/faces/1_face.jpg",
    "frames_count": 1350
  }
]
```

## ⚙️ Конфигурация

Основные настройки в `config.py`:

```python
# Пороги детекции
VIOLATION_DURATION_THRESHOLD = 3  # Секунды
BUFFER_AFTER_VIOLATION = 5        # Секунды записи после

# YOLO параметры
YOLO_CONFIDENCE = 0.5
YOLO_IOU = 0.45

# Распознавание лиц
FACE_RECOGNITION_TOLERANCE = 0.6
```

## 📚 Структура проекта

```
gavno_dlia_strelsova/
├── config.py                 # Конфигурация
├── train_model.py            # Обучение модели
├── face_database.py          # Управление базой лиц
├── detect_violations.py      # Основной скрипт детекции
├── main.ipynb               # Jupyter notebook для экспериментов
├── requirements_yolo.txt    # Зависимости
├── .env.example             # Пример переменных окружения
├── data/                    # Данные
│   └── faces_database/      # База данных лиц
├── models/                  # Модели
│   └── best.pt             # Обученная модель
└── output/                  # Результаты
    ├── videos/
    ├── faces/
    └── reports/
```

## 🎮 Управление

Во время работы:
- **Q** - остановить детекцию и сгенерировать отчет
- **Ctrl+C** - аварийная остановка

## 🔧 Требования

- Python 3.8+
- CUDA (опционально, для GPU ускорения)
- Веб-камера или видеофайлы

## 📝 Примечания

### Для Windows:
- Убедитесь, что установлен Visual C++ Build Tools (для dlib)
- Скачать: https://visualstudio.microsoft.com/visual-cpp-build-tools/

### Для работы с GPU:
```powershell
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
```

## 🐛 Решение проблем

### Ошибка установки face_recognition
```powershell
pip install cmake
pip install dlib
pip install face_recognition
```

### Ошибка с OpenCV
```powershell
pip uninstall opencv-python opencv-contrib-python
pip install opencv-contrib-python
```

## 📈 Улучшения производительности

1. **GPU ускорение**: установите CUDA-версию PyTorch
2. **Модель**: используйте YOLO11s вместо YOLO11n для большей точности
3. **Разрешение**: уменьшите размер входных изображений для скорости

## 📞 Поддержка

При возникновении проблем:
1. Проверьте версии зависимостей
2. Убедитесь, что модель обучена
3. Проверьте формат датасета

## 📄 Лицензия

MIT License

## 🙏 Благодарности

- Ultralytics YOLO
- face_recognition library
- Roboflow
