# 🎬 ОБРАБОТКА ВИДЕО - КРАТКОЕ РУКОВОДСТВО

## ✅ Система обновлена!

Теперь `detect_violations.py` поддерживает:
- ✅ Видеофайлы (mp4, avi, mov, mkv и др.)
- ✅ Пакетную обработку папок с видео
- ✅ Прогресс-бар для отслеживания
- ✅ Сохранение обработанного видео
- ✅ Работу без отображения окна (быстрее)

---

## 🚀 БЫСТРЫЙ СТАРТ

### Вариант 1: Перетащить видео на .bat файл (САМЫЙ ПРОСТОЙ)

1. Найдите файл **`process_video.bat`**
2. Перетащите на него:
   - **Видеофайл** (video.mp4) - обработает один файл
   - **Папку** с видео - обработает все видео в папке

Готово! 🎉

---

### Вариант 2: Командная строка

#### Обработать одно видео:

```powershell
# Активируйте venv
.\.venv\Scripts\Activate.ps1

# С отображением окна
python detect_violations.py --source "test_video.mp4"

# Без отображения (БЫСТРЕЕ для больших видео)
python detect_violations.py --source "test_video.mp4" --no-display

# Без сохранения обработанного видео (экономия места)
python detect_violations.py --source "test_video.mp4" --no-save
```

#### Обработать папку с видео:

```powershell
# Положите все видео в папку videos_to_process/
python detect_violations.py --batch "videos_to_process" --no-display
```

#### С пользовательской моделью:

```powershell
python detect_violations.py --source "video.mp4" --model "models/custom.pt"
```

---

## 📂 Структура файлов

### ДО обработки:

Положите видео в папку:
```
videos_to_process/
├── lesson1.mp4
├── lesson2.mp4
└── exam.mp4
```

### ПОСЛЕ обработки:

```
output/
├── processed_videos/           ← 🎬 Полные обработанные видео с аннотациями
│   ├── processed_lesson1_20251223_143022.mp4
│   └── processed_lesson2_20251223_144530.mp4
│
├── videos/                     ← 📹 Короткие видео каждого нарушения
│   ├── food_and_water_15_20251223_143045.mp4
│   └── sleep_and_phone_23_20251223_144612.mp4
│
├── faces/                      ← 👤 Фотографии лиц нарушителей
│   ├── 15_20251223_143045_face.jpg
│   └── 23_20251223_144612_face.jpg
│
└── report_20251223_145000.json ← 📊 JSON отчёт со всей информацией
```

---

## 🎯 Что детектируется

Система находит 3 типа нарушений:

| Класс | Название | Подпись на видео |
|-------|----------|------------------|
| 0 | food_and_water | 🍔 Еда/Напитки |
| 1 | sleep_and_phone | 😴 Сон/Телефон |
| 2 | something | ⚠️ Нарушение |

### Подписи на видео выглядят так:

```
🍔 Еда/Напитки 0.95 | Иван Иванов | 12с
😴 Сон/Телефон 0.87 | Петр Петров | 5с
⚠️ Нарушение 0.78 | Неизвестный | 3с
```

Где:
- **0.95** - уверенность модели (0-1)
- **Иван Иванов** - имя из базы лиц
- **12с** - длительность нарушения в секундах

---

## ⚡ Производительность

### На GPU (RTX 5070):

- **Скорость**: 100-150 FPS
- **Пример**: Видео 10 минут → обработка 1-2 минуты
- **Память**: ~4-6 GB VRAM

### На CPU:

- **Скорость**: 5-10 FPS
- **Пример**: Видео 10 минут → обработка 10-20 минут
- **Память**: ~2-4 GB RAM

---

## 📊 Параметры командной строки

```
--source PATH         Путь к видеофайлу или 0 для веб-камеры
--batch PATH          Папка с видео для пакетной обработки
--model PATH          Путь к модели (default: models/best.pt)
--no-display          Не показывать окно (быстрее для файлов)
--no-save             Не сохранять обработанное видео
```

---

## 📝 Примеры использования

### 1. Обработать урок

```powershell
python detect_violations.py --source "lesson_2023_12_23.mp4" --no-display
```

**Получите:**
- ✅ Обработанное видео: `output/processed_videos/processed_lesson_*.mp4`
- ✅ Фрагменты нарушений: `output/videos/food_and_water_*.mp4`
- ✅ Фото лиц: `output/faces/*.jpg`
- ✅ JSON отчёт: `output/report_*.json`

### 2. Обработать экзамен с распознаванием лиц

```powershell
# Сначала добавьте лица студентов
python face_database.py

# Затем обработайте видео
python detect_violations.py --source "exam_2023_12_23.mp4"
```

**В отчёте будут имена студентов!**

### 3. Пакетная обработка недели занятий

```powershell
# Положите все видео в videos_to_process/
python detect_violations.py --batch "videos_to_process" --no-display
```

**Все видео будут обработаны автоматически!**

### 4. Быстрая обработка (без окна и без сохранения)

```powershell
python detect_violations.py --source "video.mp4" --no-display --no-save
```

**Получите только фрагменты нарушений и отчёт, без полного видео.**

---

## 📈 Анализ JSON отчёта

### Структура отчёта:

```json
[
  {
    "id": 1,
    "type": "food_and_water",
    "start_time": "2023-12-23T14:30:45",
    "end_time": "2023-12-23T14:31:12",
    "duration_seconds": 27,
    "person_name": "Иван Иванов",
    "confidence": 0.89,
    "video_path": "output/videos/food_and_water_15_*.mp4",
    "face_path": "output/faces/15_*_face.jpg",
    "frames_count": 810
  }
]
```

### Анализ в Python:

```python
import json
import pandas as pd

# Загрузка отчёта
with open('output/report_20251223_145000.json', 'r', encoding='utf-8') as f:
    data = json.load(f)

df = pd.DataFrame(data)

# Статистика по студентам
print("Нарушения по студентам:")
print(df.groupby('person_name')['type'].value_counts())

# Общая длительность
total_duration = df['duration_seconds'].sum()
print(f"\nВсего времени нарушений: {total_duration / 60:.1f} минут")

# По типам
print("\nПо типам нарушений:")
print(df['type'].value_counts())
```

---

## 🔧 Настройки

### Изменить параметры в `config.py`:

```python
# Минимальная длительность нарушения (секунды)
VIOLATION_DURATION_THRESHOLD = 3.0  # Меньше = чувствительнее

# Уверенность детекции (0-1)
YOLO_CONFIDENCE = 0.5  # Выше = строже

# Буфер после нарушения (секунды)
BUFFER_AFTER_VIOLATION = 5  # Время записи после исчезновения
```

---

## ❓ FAQ

### Q: Можно ли обрабатывать в реальном времени?

**A:** Да! Для веб-камеры:
```powershell
python detect_violations.py --source 0
```

Для IP-камеры:
```powershell
python detect_violations.py --source "rtsp://username:password@camera_ip/stream"
```

### Q: Как ускорить обработку?

**A:**
1. Используйте `--no-display` (не показывать окно)
2. Используйте `--no-save` (не сохранять полное видео)
3. Убедитесь, что используется GPU (не CPU)
4. Уменьшите разрешение видео перед обработкой

### Q: Какие форматы поддерживаются?

**A:** MP4, AVI, MOV, MKV, FLV, WMV - все популярные форматы!

### Q: Как добавить студентов в базу лиц?

**A:**
```powershell
python face_database.py
```

Команды:
- `add` - добавить одно фото
- `add_folder` - добавить папку с фото
- `list` - показать всех
- `exit` - выход

---

## 🎉 ГОТОВО!

Теперь вы можете обрабатывать заранее подготовленные видео!

### Быстрый старт:

1. ✅ Положите видео в `videos_to_process/`
2. ✅ Двойной клик на **`process_video.bat`** и перетащите папку
   
   ИЛИ
   
3. ✅ `python detect_violations.py --batch "videos_to_process" --no-display`

**Готово!** 🚀

---

## 📞 Дополнительная информация

- **README.md** - Основная документация
- **COMMANDS.md** - Шпаргалка по командам
- **TRAINING_GUIDE.md** - Инструкция по обучению

Удачи! 🎬
