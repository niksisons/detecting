# ✅ TODO: Что осталось сделать перед обучением

## 🎯 БЫСТРЫЙ СТАРТ (3 шага)

### ⏳ 1. Установите оставшиеся пакеты (5 минут)

```powershell
.\.venv\Scripts\activate
pip install face-recognition roboflow
```

**Примечание для Windows:**
Если `face-recognition` не устанавливается, нужно установить Visual C++ Build Tools:
1. Скачать: https://visualstudio.microsoft.com/visual-cpp-build-tools/
2. Установить "Desktop development with C++"
3. Повторить: `pip install face-recognition`

**Альтернативный путь (если проблемы с dlib):**
```powershell
# Сначала установите cmake
pip install cmake

# Затем dlib
pip install dlib

# Затем face-recognition
pip install face-recognition
```

---

### ⏳ 2. Настройте .env файл (2 минуты)

Откройте файл `.env` в корне проекта и заполните:

```env
# Roboflow API (ОБЯЗАТЕЛЬНО!)
ROBOFLOW_API_KEY=ваш_roboflow_api_ключ
ROBOFLOW_WORKSPACE=ваш_workspace_name
ROBOFLOW_PROJECT=ваш_project_name
ROBOFLOW_VERSION=1

# GPU (уже настроено)
USE_GPU=true
```

**Где взять данные:**

1. **ROBOFLOW_API_KEY:**
   - Зайдите на https://roboflow.com
   - Settings → API
   - Скопируйте Private API Key

2. **ROBOFLOW_WORKSPACE:**
   - Ваше имя workspace на Roboflow
   - Обычно это ваш username или название организации

3. **ROBOFLOW_PROJECT:**
   - Название вашего проекта с датасетом
   - У вас уже есть датасет с классами: food_and_water, sleep_and_phone, something

4. **ROBOFLOW_VERSION:**
   - Версия вашего датасета (обычно 1)

**Пример:**
```env
ROBOFLOW_API_KEY=AbCdEf123456
ROBOFLOW_WORKSPACE=niksisons
ROBOFLOW_PROJECT=discipline-monitor
ROBOFLOW_VERSION=1
USE_GPU=true
```

---

### ⏳ 3. Запустите обучение! (30-45 минут)

```powershell
python train_model.py
```

**Что произойдет:**
1. ✅ Загрузится ваш датасет с Roboflow
2. ✅ Загрузится предобученная YOLO11n
3. ✅ Начнется обучение на **GPU RTX 5070**
4. ✅ Сохранится лучшая модель в `models/best.pt`

**Ожидайте увидеть:**
```
📥 Загрузка датасета с Roboflow...
✅ Датасет загружен

🚀 Начало обучения модели YOLO11...
Using device: cuda:0 (NVIDIA GeForce RTX 5070)

Epoch 1/100: 100%|████████| loss: 0.89
Epoch 2/100: 100%|████████| loss: 0.72
...
✅ Обучение завершено!
📊 mAP50: 0.92
```

---

## 📋 ПОЛНЫЙ ЧЕКЛИСТ

### ✅ Уже готово:
- [x] Python 3.11.9 установлен
- [x] Виртуальное окружение создано
- [x] PyTorch 2.5.1+cu121 установлен
- [x] CUDA 12.1 настроена
- [x] GPU RTX 5070 обнаружена и работает
- [x] Ultralytics YOLO установлен
- [x] OpenCV установлен
- [x] Структура проекта создана
- [x] Конфигурация оптимизирована для GPU
- [x] Batch size = 32 (для 12GB GPU)

### ⏳ Нужно сделать:
- [ ] Установить `face-recognition`
- [ ] Установить `roboflow`
- [ ] Заполнить `.env` файл с API ключами
- [ ] Запустить обучение `python train_model.py`

### 🎯 После обучения:
- [ ] Добавить студентов в базу лиц `python face_database.py`
- [ ] Протестировать детекцию `python detect_violations.py --source 0`

---

## 🚨 ВАЖНО ПЕРЕД ОБУЧЕНИЕМ

### Проверьте датасет на Roboflow:

1. **Количество изображений:**
   - Минимум: 50+ изображений на класс
   - Рекомендуется: 100+ изображений на класс
   - Идеально: 200+ изображений на класс

2. **Разметка:**
   - Все изображения размечены bounding boxes
   - Классы: `food_and_water`, `sleep_and_phone`, `something`
   - Нет пропущенных аннотаций

3. **Разделение данных:**
   - Train: 70-80%
   - Valid: 15-20%
   - Test: 10-15%

4. **Аугментация (в Roboflow):**
   - Можно добавить перед экспортом
   - Или YOLO сделает автоматически при обучении

---

## 💡 СОВЕТЫ

### 1. Мониторинг GPU во время обучения:

Откройте **второй терминал** и запустите:
```powershell
nvidia-smi -l 1
```

Вы увидите:
- Загрузку GPU: должна быть 95-100%
- Использование памяти: ~8-10GB из 12GB
- Температуру: обычно 60-75°C

### 2. Если GPU не используется:

Проверьте:
```powershell
python check_gpu.py
```

Должно показать:
```
✅ CUDA доступна: True
✅ GPU работает корректно!
```

### 3. Если Out of Memory:

В `config.py` уменьшите:
```python
TRAIN_BATCH = 16  # вместо 32
# или даже
TRAIN_BATCH = 8
```

### 4. Для ускорения обучения:

Используйте меньший размер изображений:
```python
TRAIN_IMG_SIZE = 512  # вместо 640
```

### 5. Для лучшего качества:

Увеличьте количество эпох:
```python
TRAIN_EPOCHS = 150  # вместо 100
```

---

## 📊 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

### После обучения вы получите:

```
models/
└── discipline_monitor/
    ├── weights/
    │   ├── best.pt      ← Лучшая модель
    │   └── last.pt      ← Последняя эпоха
    ├── results.png      ← Графики метрик
    ├── confusion_matrix.png
    └── predictions/     ← Примеры детекций
```

### Метрики качества:

| Метрика | Целевое значение | Описание |
|---------|------------------|----------|
| mAP@50 | >0.80 | Средняя точность |
| mAP@50-95 | >0.60 | Точность при разных порогах |
| Precision | >0.75 | Точность детекции |
| Recall | >0.70 | Полнота детекции |

---

## 🎯 ПОСЛЕ УСПЕШНОГО ОБУЧЕНИЯ

### Тестирование модели:

```powershell
# На одном изображении
python -c "from ultralytics import YOLO; model = YOLO('models/best.pt'); model.predict('test_image.jpg', save=True)"

# На веб-камере
python detect_violations.py --source 0

# На видео
python detect_violations.py --source video.mp4
```

### Добавление студентов в базу:

```powershell
python face_database.py
```

Команды:
- `add` - добавить одно фото
- `add_folder` - добавить папку с фото
- `list` - показать всех
- `exit` - выход

### Запуск системы мониторинга:

```powershell
python detect_violations.py --source 0
```

Система будет:
- ✅ Детектировать нарушения
- ✅ Распознавать лица
- ✅ Записывать видео
- ✅ Формировать отчеты

---

## 📞 ПОМОЩЬ

### Если что-то не работает:

1. **Читайте вывод в консоли** - там будут подсказки
2. **Проверьте `check_gpu.py`** - GPU должен быть доступен
3. **Проверьте `.env`** - все ключи заполнены?
4. **Смотрите документацию:**
   - `START_HERE.md` - общая информация
   - `SETUP_COMPLETE.md` - детальная настройка
   - `README_YOLO.md` - полная документация

### Логи обучения:

Сохраняются в:
```
models/discipline_monitor/
└── runs/detect/train/
```

---

## 🎉 ГОТОВО!

Теперь просто выполните 3 шага выше и начните обучение!

**Следующая команда:**
```powershell
pip install face-recognition roboflow
# Затем настройте .env
# Затем: python train_model.py
```

Удачи! 🚀
