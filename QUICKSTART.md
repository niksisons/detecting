# 🚀 Быстрый старт - Система мониторинга дисциплины

## 📋 Что вам понадобится

1. ✅ Python 3.8+ установлен
2. ✅ Веб-камера или видеофайлы для тестирования
3. ✅ Датасет на Roboflow (у вас уже есть)
4. ✅ API ключ от Roboflow

## ⚡ Установка за 5 минут

### Вариант 1: Автоматическая установка (Windows)

```powershell
# Запустите setup.bat
.\setup.bat
```

### Вариант 2: Ручная установка

```powershell
# 1. Создайте виртуальное окружение
python -m venv .venv

# 2. Активируйте его
.\.venv\Scripts\activate

# 3. Установите зависимости
pip install -r requirements_yolo.txt

# 4. Создайте .env файл
copy .env.example .env
```

## 🔑 Настройка API ключа Roboflow

Откройте файл `.env` и заполните:

```env
ROBOFLOW_API_KEY=ваш_ключ_здесь
ROBOFLOW_WORKSPACE=ваш_workspace
ROBOFLOW_PROJECT=название_проекта
ROBOFLOW_VERSION=1
```

**Где взять API ключ:**
1. Зайдите на [roboflow.com](https://roboflow.com)
2. Откройте Settings → API
3. Скопируйте Private API Key

## 🎓 Обучение модели

```powershell
python train_model.py
```

Это:
- ✅ Загрузит датасет с Roboflow
- ✅ Обучит YOLO11 на ваших данных
- ✅ Сохранит лучшую модель в `models/best.pt`

**Время обучения:**
- С GPU (CUDA): ~30-60 минут
- Без GPU: ~2-4 часа

## 👥 Добавление студентов в базу лиц

### Способ 1: Интерактивный

```powershell
python face_database.py
```

Команды:
```
add - добавить одно фото
add_folder - добавить папку с фото
list - показать всех
exit - выход
```

### Способ 2: В коде

```python
from face_database import FaceDatabase

db = FaceDatabase()
db.add_person("Иванов Иван", "photos/ivanov.jpg")
db.add_person_from_folder("Петров Петр", "photos/petrov/")
db.save_database()
```

**Рекомендации для фото:**
- 📸 Фронтальное освещение
- 👤 Лицо занимает >50% кадра
- 😊 Нейтральное выражение
- 📷 Минимум 3-5 фото на человека

## 🎬 Запуск детекции

### С веб-камерой

```powershell
python detect_violations.py --source 0
```

### С видеофайлом

```powershell
python detect_violations.py --source path\to\video.mp4
```

### С IP-камерой

```powershell
python detect_violations.py --source "rtsp://username:password@ip:port/stream"
```

### Управление

- **Q** - остановить и сохранить отчет
- **Ctrl+C** - экстренная остановка

## 📊 Просмотр результатов

После завершения детекции:

```
output/
├── videos/                    # Видео нарушений
│   ├── sleep_and_phone_1_20250123_120000.mp4
│   └── food_and_water_2_20250123_120530.mp4
├── faces/                     # Лица нарушителей
│   ├── 1_20250123_120000_face.jpg
│   └── 2_20250123_120530_face.jpg
└── report_20250123_123000.json  # Отчет
```

### Анализ отчета

```powershell
# Конвертировать в Excel
python report_utils.py output\report_XXX.json --excel

# Создать графики
python report_utils.py output\report_XXX.json --charts

# Показать статистику
python report_utils.py output\report_XXX.json --summary

# Все сразу
python report_utils.py output\report_XXX.json --all
```

## 🎯 Типичный рабочий процесс

### День 1: Настройка

```powershell
# 1. Установка
.\setup.bat

# 2. Настройка .env
notepad .env

# 3. Обучение модели
python train_model.py
```

### День 2: Подготовка базы лиц

```powershell
# 4. Добавление студентов
python face_database.py
```

### День 3+: Мониторинг

```powershell
# 5. Запуск детекции
python detect_violations.py --source 0

# 6. Анализ результатов
python report_utils.py output\report_XXX.json --all
```

## 🔧 Решение проблем

### Ошибка "ultralytics not found"

```powershell
pip install ultralytics
```

### Ошибка при установке face_recognition

```powershell
# Установите Visual C++ Build Tools
# Скачать: https://visualstudio.microsoft.com/visual-cpp-build-tools/

# Затем:
pip install cmake
pip install dlib
pip install face_recognition
```

### Ошибка CUDA / GPU не найден

```powershell
# Для работы с GPU установите CUDA-версию PyTorch:
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
```

### Низкая скорость детекции

1. **Используйте GPU** - в 10-50 раз быстрее
2. **Уменьшите разрешение** - измените `YOLO_IMG_SIZE` в `config.py`
3. **Используйте легкую модель** - YOLO11n вместо YOLO11x

## 📈 Оптимизация точности

### Если много ложных срабатываний:

```python
# В config.py увеличьте порог confidence:
YOLO_CONFIDENCE = 0.7  # вместо 0.5
```

### Если пропускает нарушения:

```python
# Уменьшите порог:
YOLO_CONFIDENCE = 0.3
```

### Улучшение распознавания лиц:

```python
# Добавьте больше фото каждого студента
FACE_RECOGNITION_TOLERANCE = 0.5  # строже
```

## 📚 Дополнительные материалы

- 📖 Полная документация: `README_YOLO.md`
- 💻 Jupyter Notebook: `main.ipynb`
- ⚙️ Настройки: `config.py`

## 🆘 Получение помощи

1. Проверьте `README_YOLO.md`
2. Изучите примеры в `main.ipynb`
3. Проверьте логи в `logs/`

## 🎉 Готово!

Теперь у вас есть полностью рабочая система мониторинга дисциплины!

**Следующие шаги:**
1. Настройте параметры в `config.py` под ваши нужды
2. Соберите больше данных для улучшения точности
3. Тестируйте на реальных занятиях

Удачи! 🚀
